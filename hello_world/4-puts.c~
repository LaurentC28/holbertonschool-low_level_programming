#include <stdio.h>
/**
* main - function
* print the main function
* Return: value nul
* void est le retour 
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle")
return (0);
}
